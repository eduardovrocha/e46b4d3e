<%= form_with(model: resource_field) do |form| %>
  <% if resource_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resource_field.errors.count, "error") %> prohibited this resource_field from being saved:</h2>

      <ul>
        <% resource_field.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
